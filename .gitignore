# macOS / editor noise
.DS_Store
*.swp
*.swo

# VS Code workspace cache (keep the JSON config files you commit)
.vscode/*
!.vscode/*.json

# Tweego build artefacts
# /docs/index.html        # published HTML (auto-generated) - commented out to allow preview builds
/export/                # if you ever output here
*.html~                # temp files if you open the game in an editor

# Twine GUI backup folder
/backups/

# Node / Python cache (if you add tooling later)
node_modules/
__pycache__/
*.py[cod]

# Git LFS or large binaries — add patterns if needed
